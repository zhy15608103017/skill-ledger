{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AI Code Review Result",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "verdict",
    "summary",
    "blocking_findings",
    "warnings",
    "verification_notes",
    "confidence"
  ],
  "properties": {
    "verdict": {
      "type": "string",
      "enum": ["pass", "fail", "needs_human"]
    },
    "verdict_label": {
      "type": "string",
      "description": "Chinese display label added by the runner in final output."
    },
    "summary": {
      "type": "string"
    },
    "blocking_findings": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/finding"
      }
    },
    "warnings": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/finding"
      }
    },
    "verification_notes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "reviewer_failures": {
      "type": "array",
      "description": "Runner-owned model invocation failures, added by the local review script rather than the reviewer model.",
      "items": {
        "$ref": "#/$defs/reviewer_failure"
      }
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    }
  },
  "$defs": {
    "finding": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "severity",
        "title",
        "file",
        "line",
        "evidence",
        "impact",
        "suggested_fix"
      ],
      "properties": {
        "severity": {
          "type": "string",
          "enum": ["P0", "P1", "P2", "P3"]
        },
        "title": {
          "type": "string"
        },
        "file": {
          "type": "string"
        },
        "line": {
          "type": ["integer", "null"],
          "minimum": 1
        },
        "evidence": {
          "type": "string"
        },
        "impact": {
          "type": "string"
        },
        "suggested_fix": {
          "type": "string"
        },
        "sources": {
          "type": "array",
          "description": "Reviewer provenance added by the runner in final output.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["reviewer", "provider", "model"],
            "properties": {
              "reviewer": {
                "type": "string",
                "enum": ["primary", "second", "requirement-auditor", "unknown"]
              },
              "provider": {
                "type": "string"
              },
              "model": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "reviewer_failure": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "phase",
        "reviewer",
        "provider",
        "model",
        "category",
        "retryable",
        "message",
        "status",
        "attempts"
      ],
      "properties": {
        "phase": {
          "type": "string",
          "enum": ["requirement_audit", "code_review", "unknown"]
        },
        "reviewer": {
          "type": "string",
          "enum": ["primary", "second", "requirement-auditor", "unknown"]
        },
        "provider": {
          "type": "string"
        },
        "model": {
          "type": "string"
        },
        "category": {
          "type": "string",
          "enum": ["timeout", "network", "auth", "rate_limit", "server", "config", "bad_response", "cli", "unknown"]
        },
        "retryable": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        },
        "status": {
          "type": ["integer", "null"]
        },
        "attempts": {
          "type": ["integer", "null"],
          "minimum": 1
        }
      }
    }
  }
}
